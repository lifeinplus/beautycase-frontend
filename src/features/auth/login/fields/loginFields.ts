import type { AuthFields } from '../../types'

export const loginFields: AuthFields = {
    username: {
        label: 'fields.username.label',
    },
    password: {
        label: 'fields.password.label',
    },
}
