export interface Lesson {
    _id?: string
    title: string
    shortDescription: string
    thumbnailUrl: string
    videoUrl: string
    fullDescription: string
}

export interface QueryResult {
    message: string
}
